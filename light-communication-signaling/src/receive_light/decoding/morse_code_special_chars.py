dot = "1"
dash = "3"
        
point = (dot + dash + dot + dash + dot + dash, ".")
comma = (dash + dash + dot + dot + dash + dash, ",")
colon = (dash + dash + dash + dot + dot + dot, ":")
question_mark = (dot + dot + dash + dash + dot + dot, "?")
apostrophe = (dot + dash + dash + dash + dash + dot, "'")
minus = (dash + dot + dot + dot + dot + dash, "-")
fraction = (dash + dot + dot + dash + dot, "/")
left_bracket = (dash + dot + dash + dash + dot, "(")
right_bracket = (dash + dot + dash + dash + dot + dash, ")")
quotation = (dot + dash + dot + dot + dash + dot, '"')
equals = (dash + dot  + dot + dot + dash, "=")
plus = (dot + dash + dot + dash + dot, "+")
multiplication = (dash + dot + dot + dash, "x")
at = (dot + dash + dash + dot + dash + dot, "@")
        
special_characters = [point, comma, colon, question_mark, apostrophe, minus, fraction,
                      left_bracket, right_bracket, quotation, equals, plus, multiplication, at]
